import React                         from "react";
import { Button, ButtonProps, View } from "react-native-ui-lib";

const ButtonVovTubeFollow = ( props: ButtonProps ) => <Button label={ "ĐĂNG KÝ" } borderRadius={0}  backgroundColor={ "rgb(204, 0, 0)" } size={ Button.sizes.xSmall } { ...props }/>


export default ButtonVovTubeFollow;
